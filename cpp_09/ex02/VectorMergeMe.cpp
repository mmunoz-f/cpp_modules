void PMergeMe::VectorMergeSort(std::vector<int>::iterator it1, size_t count)
{
	const std::vector<int> tmp(it1, it2);

	for (size_t i = 0; it1 < it2; it1++, i++)
	{
		if (*(it2 + i) < tmp[i])
		{
			it1
		}
	}
}
